Prérequis : 
    Créez le fichier .env sur la base du fichier env.example.

Pour lancer tout le projet : docker compose up -d.

Puis se rendre à l'url suivante pour voir le résultat : http://localhost:8080/